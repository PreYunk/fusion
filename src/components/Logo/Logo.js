import React from 'react';
import classes from './Logo.css';
import fusionLogo from '../../assets/SVG/logo.svg';

const logo = (props) => {
    return (
        <span className={classes.LogoHome}>FUSION</span>
    );
};

export default logo;